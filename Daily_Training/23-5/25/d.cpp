/**
 *    author:  HONG-LOU
 *    created: 2023-05-25 21:35:05
**/
#include <bits/stdc++.h>

auto main() -> int {
  std::cin.tie(nullptr) -> std::ios::sync_with_stdio(false);

	int n, c;
	std::cin >> n >> c;
	std::vector<int> a(n);
	std::map<int, int> x;
	for (int i = 0; i < n; i++) {
		std::cin >> a[i];
	}  
	std::sort(a.begin(), a.end());
	int l = 0;
	for (int i = 0; i < n; i++) {
		if (x[a[i] - '
	}
  return 0;
}